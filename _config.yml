# Build settings
markdown: kramdown
theme: minima

defaults:
  -
    scope:
      path: ""
    values:
      title: "Simon Bourrat - Vidéaste - Pilote de Drone"

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - node_modules/
  - psd/
  - Gemfile
  - Gemfile.lock
  - gulpfile.js
  - flightplan.js
